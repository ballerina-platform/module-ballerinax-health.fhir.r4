name: daily-build-patientcorrection100

on:
  schedule:
    - cron: "30 21 * * *" # 02:30 in LK time (GMT+5:30)
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/daily-build-executor.yml
    secrets: inherit
    with:
      working_dir: patientcorrection100
