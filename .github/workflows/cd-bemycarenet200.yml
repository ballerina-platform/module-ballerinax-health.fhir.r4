name: cd-bemycarenet200

on:
  workflow_dispatch:
    inputs:
      bal_central_environment:
        description: Ballerina Central Environment
        type: choice
        options:
        - STAGE
        - DEV
        - PROD
        required: true

jobs:
  build:
     uses: ./.github/workflows/build-executor.yml
     secrets: inherit
     with:
      working_dir: ./belgiumfederal/bemycarenet200
      bal_central_environment: ${{ inputs.bal_central_environment }}